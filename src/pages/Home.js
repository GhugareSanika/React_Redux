import React from 'react';
import CustomizedTables from '../styles/CustomizedTables'; // Ensure the path is correct

const Home = () => {
    return (
        <div>
            <CustomizedTables />
        </div>
    );
};

export default Home;
